pipeline {

  stages {
  
    stage (check java version) {
	  steps {
	    sh 'java --version'
	  }
	}
    stage (check java version) {
	  steps {
	    sh 'java --version'
	  }
    }
	stage (check git) {
	  steps {
	    sh 'git --version'
	  }
	}
  }
}
